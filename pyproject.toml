[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pautacf"
version = "0.1.0"
description = "Automação da pauta de audiências semanal — Calmon & Freitas Advogados"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
